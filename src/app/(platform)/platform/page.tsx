import type { Metadata } from "next";
import Link from "next/link";
import { AlertTriangle, ArrowRight, Building2, GraduationCap, Plus, TestTube2 } from "lucide-react";
import { PlatformHeader, StatCard, StatusBadge } from "@/components/platform/platform-ui";
import { getPlatformMetrics } from "@/lib/services/platform";

export const metadata: Metadata = { title: "Platform overview | GetDarsgah" };
export default async function PlatformPage() { const metrics = await getPlatformMetrics(); return <><PlatformHeader eyebrow="GetDarsgah control" title="Platform overview" description="Monitor schools, subscriptions, and tenant health from one operational view." action={<Link href="/platform/schools/new" className="inline-flex h-11 items-center gap-2 rounded-xl bg-primary px-5 text-sm font-bold text-white shadow-button"><Plus className="h-4 w-4" />Add school</Link>} /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Managed schools" value={metrics.total} note="Excludes archived tenants" icon={<Building2 className="h-4 w-4" />} /><StatCard label="Active subscriptions" value={metrics.active} note="Schools currently operating" icon={<Building2 className="h-4 w-4" />} /><StatCard label="Trials" value={metrics.trials} note="Schools in evaluation" icon={<TestTube2 className="h-4 w-4" />} /><StatCard label="Needs attention" value={metrics.attention} note="Suspended or past due" icon={<AlertTriangle className="h-4 w-4" />} /></div><section className="mt-8 rounded-[20px] border border-slate-200 bg-white"><div className="flex items-center justify-between border-b border-slate-200 p-6"><div><h2 className="font-bold text-ink">Recently added schools</h2><p className="mt-1 text-xs text-muted">Latest tenants across the platform</p></div><Link href="/platform/schools" className="flex items-center gap-2 text-xs font-bold text-primary">View all <ArrowRight className="h-3.5 w-3.5" /></Link></div><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left"><thead><tr className="text-[10px] uppercase tracking-wider text-muted"><th className="px-6 py-3">School</th><th className="px-6 py-3">Status</th><th className="px-6 py-3">Plan</th><th className="px-6 py-3">Students</th><th className="px-6 py-3"></th></tr></thead><tbody>{metrics.schools.slice(0, 6).map(school => <tr className="border-t border-slate-100 text-sm" key={school.id}><td className="px-6 py-4"><p className="font-bold text-ink">{school.name}</p><p className="mt-1 text-[11px] text-muted">{school.slug}</p></td><td className="px-6 py-4"><StatusBadge status={school.platform_status} /></td><td className="px-6 py-4 capitalize text-muted">{school.subscription_plan}</td><td className="px-6 py-4"><span className="inline-flex items-center gap-2"><GraduationCap className="h-4 w-4 text-muted" />{school.student_count}</span></td><td className="px-6 py-4 text-right"><Link className="text-xs font-bold text-primary" href={`/platform/schools/${school.id}`}>Manage</Link></td></tr>)}</tbody></table></div></section></>; }
