# Predeployment security checks

Code review date: 2026-09-20. This is a repository check. Production infrastructure and Supabase project settings must be verified against the actual deployment.

Current deployment targets supplied by the owner: `https://darsgah.vercel.app` and Supabase project `hgomjumrwdwzkkvsppjs` (`https://hgomjumrwdwzkkvsppjs.supabase.co`). The public sign-in page was reachable in a browser on 2026-09-20. The Supabase dashboard did not render project settings in the available browser session, and direct HTTP probes from the workspace were blocked, so response headers, project CORS/redirect configuration, RLS, TLS policy, and port exposure have not been verified live.

| Check | Repository result | Fix and remaining deployment requirement |
| --- | --- | --- |
| 1. Environment variables | **Fail in the current local configuration; guarded in code.** | Production build/start now rejects missing or placeholder Supabase URL and keys, app URL, parent session secret, rate-limit secret, and contact mail settings. Both public URLs must use HTTPS, signing secrets must be at least 32 characters, and public/service keys must differ. `.env.example` now lists the supplied public deployment URLs; its key and secret entries remain placeholders. The current `.env.local` has a non-HTTPS app URL and lacks the parent, rate-limit, and mail variables. Set real values in the deployment environment; do not copy local values. Optional Supabase CLI integrations in `supabase/config.toml` use environment references and are not application requirements. |
| 2. Debug code | **Pass for the runtime source after cleanup.** | Removed unused scratch queries, formatting experiments and remote-user diagnostic scripts. No runtime `console.log`, `console.debug`, `debugger`, security TODO/FIXME, public signup, or `/test`, `/debug`, `/admin-backdoor`, `/seed-data` route was found. Local CLI diagnostics still print nonpersonal status information; tests and local seed are outside the deployed Next.js route tree. |
| 3. Error responses | **Partially fixed.** | API route failures now use generic messages and correlation IDs; root error boundaries show a generic message and a reference digest/ID. Many server actions now return a generic message and reference ID. Some expected validation/auth failures remain plain messages without IDs, and unhandled infrastructure errors still rely on Next.js production sanitization. Review these before claiming universal coverage. Detailed server logs are limited to correlation ID, error class, and code to avoid logging personal data. |
| 4. Security headers | **Configured; live response check pending.** | `next.config.ts` adds `nosniff`, `DENY`, one-year HSTS, referrer policy, and a baseline CSP to all routes. Middleware adds a per-request nonce CSP to dynamic pages and API responses; root layout renders per request so Next.js can attach nonces to scripts. Verify HTML, API, redirect, static asset, and error responses after deployment, and inspect browser CSP reports for legitimate blocked resources. |
| 5. Auth rate limiting | **Implemented; migration/live test pending.** | Sign-in and parent sign-in consume at most five attempts per IP per minute; password-reset requests consume at most three per IP per hour. Reauthentication for password changes and account deletion also shares the login limit. Counters are atomic in Supabase via `202609200002_auth_rate_limits.sql`, keyed by HMAC of the IP. The limiter fails closed if its RPC is unavailable. Apply the migration and test limits before traffic. No public signup or application OTP endpoint exists. |
| 6. CORS | **Pass for application code; hosted provider check pending.** | No wildcard `Access-Control-Allow-Origin` header or custom CORS middleware was found. Application APIs are same-origin. Check Supabase project `hgomjumrwdwzkkvsppjs` redirect/site origins and any edge/CDN header overrides against `https://darsgah.vercel.app`. Password-reset redirects require `https://darsgah.vercel.app/reset-password`. |
| 7. Database transport and exposure | **Application transport guarded; infrastructure unverified.** | The app has no direct PostgreSQL/MongoDB client or database URL. Production Supabase API URL must use HTTPS. Local Supabase CLI config exposes port 54322 with local demo credentials; it is for development only. Confirm the production database's SSL enforcement, credentials, firewall/network rules, and lack of public unauthenticated access in the Supabase project and hosting control plane. Also verify RLS on all deployed tables. |

## Required release gates

1. Set all required production environment variables and confirm `npm run build` and `npm start` succeed.
2. Apply migrations `202609200001` through `202609200003` and `202609210001` through `202609210003` in order before enabling sign-in, payments, or account deletion.
3. Verify rate limits, response headers, CSP behavior, RLS, CORS origins, TLS, and database port exposure against the deployed endpoints.
4. Resolve the separate personal-data issues in `DATA_FLOW_AUDIT.md`, especially browser-readable Supabase auth cookies and parent portal CNIC/date-of-birth authentication.
5. Complete the authentication, authorization, payment, and input findings in `CRITICAL_PATH_SECURITY_AUDIT.md`, including hosted 15-minute OTP/JWT settings and existing SVG asset cleanup.
6. Review `ATTACK_PATH_AUDIT.md` and reconcile existing financial records. The current public deployment still lacks the repository's CSP, frame, and `nosniff` headers, so deploy and verify those changes.
